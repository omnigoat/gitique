

var javascript = {
	class_page: {
		'div.function': {
			'fn <- '
		}
	}
	"function": function() {
		var directive = {
			"div.something": {
				"fn <- funtions":
				{
					"h1": "fn.name",
					"span.version": "fn.version",
					"span.synopsis": "fn.synopsis",
					"li.signature": {
						"sig <- signatures": {
							".": "sig"
						}
					}
					
				}
			}
		};
	}
}



var jaja = {};

jaja.Array =
{
	functions: [
		{
			name: "each",
			version: 1,
			
			synopsis: "applies a function against each element in sequence",
			
			signatures: [
				"each(fn, /callbacks/)",
				"each(options, fn, /callbacks/)"
			],

			parameters: {
				options: "an options object from $[jaja.Sequence#options]",
				
				fn: {
					v1: {
						_type: "javascript.function",
						is: "the function that will be applied to each element",
						signature: "function(element, index, array)",
						remarks: "/this/ will be "
					},

					v1_1: {
						
					}

				callbacks: {
					
				}
			}
		}
	}
};



